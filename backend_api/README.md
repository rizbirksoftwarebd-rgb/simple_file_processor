# Simple OOP File Processor API
## Run locally
1. cd backend_api
2. pip install -r requirements.txt
3. export API_KEY='yourkey' (Windows: set API_KEY=yourkey)
4. uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
